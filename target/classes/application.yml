server:
  port: 9999
  context-path: /pay

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/interview
  jpa:
    hibernate:
      ddl-auto: update
    database: mysql
